apiVersion: v1
kind: Pod
metadata:
  name: load-test
  namespace: scaling-lab
spec:
  containers:
  - name: load-test
    image: busybox
    command:
    - /bin/sh
    - -c
    - |
      APP_ROUTE="http://cakephp-app:8080"
      echo "Starting load test against $APP_ROUTE"
      for i in $(seq 1 1000); do
        for j in $(seq 1 10); do
          wget -q -O- $APP_ROUTE &
        done
        sleep 1
        echo "Completed batch $i"
      done
      echo "Load test completed"
  restartPolicy: Never
