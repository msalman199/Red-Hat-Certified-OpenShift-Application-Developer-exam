apiVersion: v1
kind: Pod
metadata:
  name: load-test
spec:
  containers:
  - name: load-test
    image: busybox
    command: ["/bin/sh"]
    args: ["-c", "while true; do wget -q -O- http://webapp/; done"]
  restartPolicy: Never
